# frozen_string_literal: true

source "https://rubygems.org"

# git_source(:github) {|repo_name| "https://github.com/ma-pavone" }

    gem 'cucumber'
    gem 'capybara'
    gem 'capybara'
    gem 'capybara-screenshot'
    gem 'capybara-selenium'
    gem 'selenium-webdriver'
    gem 'selenium-webdriver'
    gem 'site_prism'
    gem 'rubocop', require: true # comando rubocop para linter, consistencia e 'offenses' do codigo
    gem "blabla" # Geraçao de CPF, CNPJ, CC, pessoa, ISBN, CEP, end, tel, senha, email
    gem 'rspec'
    gem 'rspec-retry'
    # gem 'capybara_table' #torna possivel: find(:table_row, {"First Name" => "Jonas", "Last Name" => "Nicklas"}, {})
# within :table, "People" do assert_selector(:table_row, {"First Name" => "Jonas", "Last Name" => "Nicklas"}, {}) end
    # gem 'byebug'